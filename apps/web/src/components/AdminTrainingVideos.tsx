/* Thin re-export kept at the original path so the lazy import in
 * AdminConsole.tsx keeps working. The deck content lives in
 * ./trainingDecks/admin.tsx and the shared shell in ./TrainingVideoLibrary.tsx. */
export { AdminDocumentationModal } from "./trainingDecks/admin";
export type { AdminTrainingVideo } from "./trainingDecks/admin";
