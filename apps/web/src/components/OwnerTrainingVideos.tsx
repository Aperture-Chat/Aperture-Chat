/* Thin re-export kept at the original path so the lazy import in
 * PlatformConsole.tsx keeps working. The deck content lives in
 * ./trainingDecks/owner.tsx and the shared shell in ./TrainingVideoLibrary.tsx. */
export { OwnerDocumentationModal } from "./trainingDecks/owner";
export type { OwnerTrainingVideo } from "./trainingDecks/owner";
